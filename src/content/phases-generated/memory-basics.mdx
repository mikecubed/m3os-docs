---
phase: 2
title: "Memory Basics"
status: complete
category: foundations
summary: "Move from a static early kernel into a kernel that can allocate memory safely and reason about physical and virtual address space."
learningGoal: "Understand the memory map provided by the bootloader."
buildsOn:
  - "boot-foundation"
extends: []
replaces: []
components:
  - "memory-manager"
learningObjectives:
  - "Understand the memory map provided by the bootloader."
  - "Learn the difference between physical frames and virtual pages."
  - "Add a minimal heap without losing track of unsafe boundaries."
keyFiles:
  - path: "kernel-core/src/buddy.rs"
    summary: "The frame allocator uses a buddy allocator (kernel-core/src/buddy.rs) with 10 order"
  - path: "kernel-core/src/slab.rs"
    summary: "Slab allocator (kernel-core/src/slab.rs) provides fixed-size caches on top of the"
codeSpotlights:
  - title: "Buddy"
    file: "kernel-core/src/buddy.rs"
    lines: "See file"
    summary: "The frame allocator uses a buddy allocator (kernel-core/src/buddy.rs) with 10 order"
    githubUrl: "https://github.com/mikecubed/m3os/blob/main/kernel-core/src/buddy.rs"
  - title: "Slab allocator (kernel-core/src/slab.rs)"
    file: "kernel-core/src/slab.rs"
    lines: "See file"
    summary: "Slab allocator (kernel-core/src/slab.rs) provides fixed-size caches on top of the"
    githubUrl: "https://github.com/mikecubed/m3os/blob/main/kernel-core/src/slab.rs"
realOsDifferences:
  - "Real kernels support far more memory states, NUMA, large pages, reclaim strategies, copy-on-write, and demand paging. For a toy OS, a fixed heap and a simple allocator are better because they teach the mechanics without hiding them behind complex policy."
successCriteria:
  - "`Box`, `Vec`, and small dynamic data structures work after heap init."
  - "Frame allocation does not reuse already claimed memory."
  - "Known virtual addresses can be translated for debugging."
  - "Memory setup is documented clearly enough to revisit later."
---

## Why this phase matters

Move from a static early kernel into a kernel that can allocate memory safely and reason about physical and virtual address space.

## How it works in m3OS

Memory management is one of the first things the kernel must set up after boot. It has three layers:

## How it builds on earlier phases

This phase builds on Boot Foundation and turns that foundation into Memory Basics.

## Implementation outline

- Store a `'static` reference to the bootloader memory map (the regions slice is valid for the kernel's lifetime) and parse it into typed kernel structures.
- Implement a simple frame allocator with conservative rules.
- Expose page-table operations through safe wrappers around `x86_64`.
- Map a small heap and initialize the allocator.
- Add simple allocation tests and logging for confidence.

## Task progress snapshot

12 of 12 tracked tasks are complete.

## Documentation targets

- explain how usable frames are selected from the boot memory map
- explain the heap layout and why it is fixed-size at first
- document which parts of paging require `unsafe` and why

## Deferred follow-on work

- reclaiming freed physical frames
- demand paging
- sophisticated virtual memory policies

## Source materials

- [Roadmap entry](https://github.com/mikecubed/m3os/blob/main/docs/roadmap/02-memory-basics.md)
- [Implementation document](https://github.com/mikecubed/m3os/blob/main/docs/03-memory.md)
- [Task list](https://github.com/mikecubed/m3os/blob/main/docs/roadmap/tasks/02-memory-basics-tasks.md)
