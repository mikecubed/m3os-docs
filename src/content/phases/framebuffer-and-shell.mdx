---
phase: 9
title: "Framebuffer And Shell"
status: complete
category: productivity
summary: "Make the OS pleasant to interact with by adding on-screen text output and a very small shell for manual exploration."
learningGoal: "Understand how UI can remain a userspace concern in a microkernel."
buildsOn:
  - "storage-and-vfs"
extends: []
replaces: []
components:
  - "terminal"
learningObjectives:
  - "Understand how UI can remain a userspace concern in a microkernel."
  - "Learn the basics of text rendering, input handling, and command dispatch."
  - "End with a system that feels like an operating system rather than a kernel demo."
keyFiles: []
codeSpotlights:
  - title: "Parse framebuffer information for text-mode rendering"
    file: "kernel/src/fb/mod.rs"
    lines: "L468-L488"
    summary: "Parse framebuffer information for text-mode rendering. Why it matters: The framebuffer info from the bootloader must be parsed before any pixels can be drawn. Focus on `init` in `kernel/src/fb/mod.rs` while you trace the snippet."
    githubUrl: "https://github.com/mikecubed/m3os/blob/v0.9.0/kernel/src/fb/mod.rs"
    snippet: "/// Initialise the framebuffer text console.\n///\n/// Called once from `kernel_main` before `task::run()`.  Extracts the raw\n/// pointer and layout from `fb`, stores them in the global [`CONSOLE`], and\n/// clears the screen.\n///\n/// # Safety\n/// `fb` must be `&'static mut` (derived from `boot_info.framebuffer`).  The\n/// caller must not access `boot_info.framebuffer` again after this call.\n///\n/// Returns `true` if the framebuffer was large enough to enable the text console.\npub fn init(fb: &'static mut FrameBuffer) -> bool {\n    let info: FrameBufferInfo = fb.info();\n    // Extract the raw mutable pointer from the &'static mut FrameBuffer.\n    // SAFETY: fb is &'static mut so the pointer is valid for the kernel\n    // lifetime.  We store it inside a Mutex<Option<FbConsole>> so it is\n    // only ever accessed with the lock held, preventing aliased writes.\n    let buf_ptr: *mut u8 = fb.buffer_mut().as_mut_ptr();\n    // SAFETY: buf_ptr and info are both derived from &'static mut FrameBuffer.\n    unsafe { init_from_parts(buf_ptr, info) }\n}"
    snippetLanguage: "rust"
  - title: "Implement line input with basic editing"
    file: "userspace/shell/src/main.rs"
    lines: "L41-L64"
    summary: "Implement line input with basic editing. Why it matters: Interactive use requires at minimum line-buffered input with backspace support. Focus on `read_line` in `userspace/shell/src/main.rs` while you trace the snippet."
    githubUrl: "https://github.com/mikecubed/m3os/blob/v0.9.0/userspace/shell/src/main.rs"
    snippet: "/// Read a line from stdin, handling backspace and character echo.\n/// Returns the number of valid bytes (excluding null terminator).\nfn read_line(buf: &mut [u8; MAX_LINE]) -> usize {\n    let mut pos = 0usize;\n    loop {\n        let mut byte = [0u8; 1];\n        let n = read(STDIN_FILENO, &mut byte);\n        if n <= 0 {\n            // EOF or error — return what we have.\n            if pos > 0 {\n                return pos;\n            }\n            // Yield CPU briefly (~10ms) and retry.\n            syscall_lib::nanosleep(1);\n            continue;\n        }\n\n        let ch = byte[0];\n\n        if ch == b'\\n' || ch == b'\\r' {\n            // Echo newline (stdin_feeder sends \\n but doesn't echo it).\n            let _ = write(STDOUT_FILENO, b\"\\n\");\n            return pos;\n        }"
    snippetLanguage: "rust"
realOsDifferences:
  - "Real systems have richer terminal stacks, GPU drivers, process launch models, shells, libraries, and compatibility layers."
  - "Production terminal emulators handle hundreds of escape sequences and support Unicode."
  - "A learning system should stop once text I/O and a few commands make the service architecture visible and satisfying to use."
successCriteria:
  - "Text appears on screen in addition to serial output."
  - "Keyboard input reaches the shell through userspace services."
  - "The shell can run a handful of built-in commands."
  - "File-oriented commands such as `ls` and `cat` exercise the VFS path."
---

## Why this phase matters

Make the OS pleasant to interact with by adding on-screen text output and a very small shell for manual exploration.

## How it works in m3OS

Phase 9 adds the first interactive layer to m³OS: a pixel framebuffer text console, keyboard IPC, and a tiny shell with built-in file commands. The additions are:

## How it builds on earlier phases

This phase builds on Storage And VFS and turns that foundation into Framebuffer And Shell.

## Implementation outline

- Parse framebuffer information from boot data and expose a simple drawing API.
- Add text rendering with a fixed bitmap font.
- Extend the console service to own the visible terminal state.
- Build a tiny shell that speaks only to documented services.
- Keep the command model simple and text-oriented.

## Task progress snapshot

16 of 16 tracked tasks are complete.

## Documentation targets

- Documentation deliverables have not been listed yet.

## Deferred follow-on work

- Pipes and redirection
- Job control
- Full-screen applications
- Advanced graphics or windowing

## Source materials

- [Roadmap entry](https://github.com/mikecubed/m3os/blob/main/docs/roadmap/09-framebuffer-and-shell.md)
- [Implementation document](https://github.com/mikecubed/m3os/blob/main/docs/09-framebuffer-and-shell.md)
- [Task list](https://github.com/mikecubed/m3os/blob/main/docs/roadmap/tasks/09-framebuffer-and-shell-tasks.md)
