---
phase: 20
title: "Userspace Init Shell"
status: complete
category: userspace
summary: "Replace the kernel-resident `init_task` and `shell_task` ring-0 functions with real ring-3 userspace processes. After this phase PID 1 is a `no_std` Rust binary loaded from the ramdisk; the interactive shell is a userspace ELF that init spawns. The kernel is no longer responsible for parsing commands or managing the interactive session."
learningGoal: "Understand the Unix PID 1 contract: why init must never exit and must reap orphaned children."
buildsOn:
  - "signal-handlers"
extends: []
replaces: []
components:
  - "boot-path"
  - "process-model"
  - "terminal"
learningObjectives:
  - "Understand the Unix PID 1 contract: why init must never exit and must reap orphaned children."
  - "See how a kernel transitions from running its own code to handing control to a userspace binary as the primary interactive process."
  - "Learn how a minimal shell uses `fork`, `execve`, `waitpid`, `dup2`, and `pipe` without depending on libc."
  - "See what `no_std` userspace Rust looks like in practice: syscall wrappers, manual stack setup, and `#[panic_handler]`."
  - "Understand how pipe fd plumbing works at the kernel level: two file descriptors pointing at the same kernel buffer, one for reading and one for writing, with EOF triggered when the last write end closes."
  - "See why the shell must close both ends of a pipe in the parent process after forking, or the child reader will never see EOF."
keyFiles:
  - path: "userspace/init"
    summary: "userspace/init: minimal no_std Rust binary; sets up stdin/stdout/stderr"
  - path: "userspace/shell"
    summary: "userspace/shell: interactive no_std Rust shell; reads stdin byte by byte"
  - path: "kernel/src/main.rs"
    summary: "Remove shell_task() and init_task() from kernel/src/main.rs; stdin_feeder_task"
  - path: "userspace/init/src/main.rs"
    summary: "Implement userspace/init/src/main.rs: open /dev/console as fds 0, 1, 2; fork"
codeSpotlights:
  - title: "Add raw syscall wrappers (syscall3–syscall6)"
    file: "userspace/syscall-lib/src/lib.rs"
    lines: "L61-L69"
    summary: "**Symbol:** syscall3, syscall4, syscall5, syscall6"
    githubUrl: "https://github.com/mikecubed/m3os/blob/v0.20.0/userspace/syscall-lib/src/lib.rs"
    snippet: "/// Raw three-argument syscall.\n#[inline(always)]\npub unsafe fn syscall3(num: u64, a0: u64, a1: u64, a2: u64) -> u64 {\n    let mut rax = num;\n    asm!(\n        \"syscall\",\n        inlateout(\"rax\") rax,\n        in(\"rdi\") a0,\n        in(\"rsi\") a1,"
    snippetLanguage: "rust"
  - title: "Create init crate"
    file: "userspace/init/Cargo.toml"
    lines: "See file"
    summary: "**Why it matters:** Init is the first userspace process; without it there is no process tree."
    githubUrl: "https://github.com/mikecubed/m3os/blob/v0.20.0/userspace/init/Cargo.toml"
  - title: "Create init entry point"
    file: "userspace/init/src/main.rs"
    lines: "L19-L27"
    summary: "**Symbol:** _start"
    githubUrl: "https://github.com/mikecubed/m3os/blob/v0.20.0/userspace/init/src/main.rs"
    snippet: "\n#[no_mangle]\npub extern \"C\" fn _start() -> ! {\n    // Fds 0/1/2 are pre-opened by the kernel for PID 1.\n    write_str(STDOUT_FILENO, \"\\nm3OS init (PID 1)\\n\");\n\n    // Spawn the first shell.\n    let mut shell_pid = spawn_shell();\n    if shell_pid < 0 {"
    snippetLanguage: "rust"
realOsDifferences:
  - "Production init systems (systemd, OpenRC, s6) handle service supervision, dependency ordering, socket activation, cgroups, and logging."
  - "Even BusyBox's `init` handles `/etc/inittab` respawn entries, runlevels, and `sysvinit` compatibility."
  - "Real shells (dash, bash) implement full POSIX parameter expansion, here-documents, arithmetic, function definitions, and complex job control."
  - "This phase targets only the subset needed to run interactive commands and pipelines -- enough to feel like a real shell without the decades of accumulated specification compliance."
successCriteria:
  - "The OS boots and presents an interactive shell prompt without any ring-0 command parsing code running."
  - "`ps` (or `cat /proc/1/status` if available) shows PID 1 is `init` and PID 2 is `sh`."
  - "`echo hello world` prints correctly."
  - "`ls | cat` produces the directory listing via a two-stage pipe."
  - "`cat /sbin/init > /dev/null` exercises I/O redirection without crashing."
  - "`cd /bin && pwd` prints `/bin`."
  - "`Ctrl-C` during a long-running child kills the child but leaves the shell running."
  - "Orphaned grandchildren are reaped by PID 1 (not left as zombies indefinitely)."
  - "`exit` in the shell causes init to re-spawn it (or the boot stops gracefully)."
  - "`kernel/src/main.rs` no longer contains `shell_task` or `init_task` functions."
  - "A command that does not exist (e.g. `notacommand`) causes `execve` to fail; the shell prints an error and returns to the prompt without crashing."
  - "Running `true` exits 0; running `false` exits 1; the shell prints nothing extra for `true` and prints a non-zero exit notice for `false`."
---

## Why this phase matters

Replace the kernel-resident `init_task` and `shell_task` ring-0 functions with real ring-3 userspace processes. After this phase PID 1 is a `no_std` Rust binary loaded from the ramdisk; the interactive shell is a userspace ELF that init spawns. The kernel is no longer responsible for parsing commands or managing the interactive session.

## How it works in m3OS

The roadmap entry defines the learning scope for this phase; a deeper implementation doc has not been matched yet.

## How it builds on earlier phases

This phase builds on Signal Handlers and turns that foundation into Userspace Init Shell.

## Implementation outline

- Uncomment `userspace/init` and `userspace/shell` in the workspace `Cargo.toml` and verify each crate has `#![no_std]`, a `#[panic_handler]`, and a `_start` entry point that calls into Rust (or a thin asm stub that sets up `rbp` and calls `main`).
- Write the syscall shim layer shared by both binaries: inline-asm wrappers for `read`, `write`, `fork`, `execve`, `waitpid`, `exit`, `pipe`, `dup2`, `open`, `close`, `chdir`, `kill`, `getpid`.
- Implement `userspace/init/src/main.rs`: open `/dev/console` as fds 0, 1, 2; fork and exec `/bin/sh`; enter an infinite `waitpid(-1, WNOHANG)` + `pause` loop.
- Implement the line-reader in `userspace/shell`: read one byte at a time from fd 0 until `\n` or `\r`; handle backspace; cap line length at 256 bytes.
- Implement the tokenizer: split on whitespace, respect single-quoted strings, expand nothing else (no glob, no variable expansion in this phase).
- Implement command dispatch: detect `cd` and `exit` builtins; otherwise `fork` + `execve`; parent calls `waitpid` for foreground; print exit status on non-zero.
- Implement pipe support: detect `|` token; allocate a `pipe` fd pair; fork left child with `dup2(pipe_write, STDOUT_FILENO)`; fork right child with `dup2(pipe_read, STDIN_FILENO)`; close both pipe ends in the parent; `waitpid` both.
- Implement `>` and `<` redirection: open the target file before `fork`; after `fork` in the child, `dup2` onto stdout or stdin; close the extra fd; exec.
- In `kernel_main`, after all kernel-task servers have been started, load `/sbin/init` from the ramdisk via the ELF loader, allocate a new address space, and transfer to ring-3 as PID 1. Remove the `init_task()` and `shell_task()` function calls.
- Add the two ELF binaries to the xtask ramdisk assembly step so they appear at `/sbin/init` and `/bin/sh` in the filesystem image.
- Validate end-to-end: boot in QEMU, observe the shell prompt, run `echo hello`, run `ls | cat`, run `cat /etc/motd > /tmp/out`, run `cd /bin && pwd`.
- Confirm that the `stdin_feeder_task` kernel task (or equivalent) correctly feeds keyboard bytes into the PID 1 process's stdin fd, so that the shell's `read(0, ...)` syscall blocks until a key is pressed rather than spinning.
- Run `cargo xtask check` to confirm no new clippy warnings are introduced by the two new workspace members before declaring the phase complete.

## Task progress snapshot

73 of 73 tracked tasks are complete.

## Documentation targets

- Documentation deliverables have not been listed yet.

## Deferred follow-on work

- Moving kernel-task servers (`console_server`, `kbd_server`, `vfs_server`, `fat_server`) to ring-3 userspace (requires the capability-grant IPC phase)
- PTY / TTY line discipline (`/dev/pts`, `termios`, raw mode, character echo)
- Job control: `SIGTSTP`, `SIGCONT`, `fg`, `bg`, process groups, sessions
- Multi-user login and `/etc/passwd`
- Shell scripting: loops, conditionals, functions, variable assignment
- Environment variable export and `$VAR` expansion
- Tab completion and readline-style editing
- `exec` builtin (replace shell image) and `source` / `.`
- Pipelines longer than two stages
- Subshell expansion `$(...)` and backtick substitution
- Here-documents and here-strings
- Stderr redirection (`2>`, `2>&1`) and fd duplication beyond 0/1/2

## Source materials

- [Roadmap entry](https://github.com/mikecubed/m3os/blob/main/docs/roadmap/20-userspace-init-shell.md)
- [Implementation document](https://github.com/mikecubed/m3os/blob/main/docs/20-userspace-init.md)
- [Task list](https://github.com/mikecubed/m3os/blob/main/docs/roadmap/tasks/20-userspace-init-shell-tasks.md)
