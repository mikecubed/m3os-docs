---
phase: 6
title: "IPC Core"
status: complete
category: foundations
summary: "Turn the kernel into a real microkernel by making userspace services communicate through explicit message passing and capabilities."
learningGoal: "Understand why microkernels move services out of ring 0."
buildsOn:
  - "userspace-entry"
extends: []
replaces: []
components:
  - "ipc"
  - "scheduler"
learningObjectives:
  - "Understand why microkernels move services out of ring 0."
  - "Learn synchronous rendezvous IPC semantics."
  - "Introduce capability tables as the security boundary."
keyFiles:
  - path: "kernel/src/arch/x86_64/syscall.rs"
    summary: "authoritative mapping lives in kernel/src/arch/x86_64/syscall.rs, and"
  - path: "userspace/syscall-lib"
    summary: "userspace uses it via userspace/syscall-lib. Treat the table above as"
  - path: "kernel/src/ipc/mod.rs"
    summary: "kernel/src/ipc/mod.rs — module overview and syscall dispatcher"
  - path: "kernel/src/ipc/endpoint.rs"
    summary: "kernel/src/ipc/endpoint.rs — rendezvous endpoint implementation"
  - path: "kernel/src/ipc/notification.rs"
    summary: "kernel/src/ipc/notification.rs — async notification objects"
  - path: "kernel/src/ipc/capability.rs"
    summary: "kernel/src/ipc/capability.rs — per-task capability table"
  - path: "kernel/src/task/scheduler.rs"
    summary: "kernel/src/task/scheduler.rs — IPC scheduler primitives"
codeSpotlights:
  - title: "Define kernel IPC objects for endpoints and notifications"
    file: "kernel/src/ipc/message.rs"
    lines: "L20-L34"
    summary: "Define kernel IPC objects for endpoints and notifications. Why it matters: These are the kernel objects through which all inter-process communication flows — endpoints for synchronous rendezvous, notifications for async signals. Focus on `Message` and `Endpoint` in `kernel/src/ipc/message.rs` while you trace the snippet."
    githubUrl: "https://github.com/mikecubed/m3os/blob/v0.6.0/kernel/src/ipc/message.rs"
    snippet: "/// A small, register-sized IPC message.\n///\n/// `label` identifies the operation (similar to a method selector in Mach or\n/// a message tag in seL4).  `data` carries up to four 64-bit words of inline\n/// payload — enough for most control operations.\n///\n/// Capability grants are deferred to Phase 7+.  For now, if a server needs to\n/// share memory with a client it must use a pre-arranged shared address.\n#[derive(Debug, Clone, Copy, Default)]\npub struct Message {\n    /// Operation identifier, chosen by convention between sender and receiver.\n    pub label: u64,\n    /// Inline data payload — up to 4 machine words.\n    pub data: [u64; 4],\n}"
    snippetLanguage: "rust"
  - title: "Add a per-process capability table with validation"
    file: "kernel/src/ipc/capability.rs"
    lines: "L83-L89"
    summary: "Add a per-process capability table with validation. Why it matters: Every IPC operation must go through capability validation — a process can only use handles it legitimately holds, preventing capability forgery. Focus on `CapabilityTable` and `CapHandle` in `kernel/src/ipc/capability.rs` while you trace the snippet."
    githubUrl: "https://github.com/mikecubed/m3os/blob/v0.6.0/kernel/src/ipc/capability.rs"
    snippet: "/// Fixed-size per-process capability table (64 slots).\n///\n/// `None` means the slot is empty.  Slots are filled on [`insert`][Self::insert]\n/// and cleared on [`remove`][Self::remove].\npub struct CapabilityTable {\n    slots: [Option<Capability>; Self::SIZE],\n}"
    snippetLanguage: "rust"
  - title: "Implement blocking recv and send primitives"
    file: "kernel/src/ipc/endpoint.rs"
    lines: "L132-L155"
    summary: "Implement blocking recv and send primitives. Why it matters: Synchronous IPC requires the ability to block the calling task until a partner arrives on the endpoint, then transfer the message and wake both sides. Focus on `recv_msg` and `send_msg` in `kernel/src/ipc/endpoint.rs` while you trace the snippet."
    githubUrl: "https://github.com/mikecubed/m3os/blob/v0.6.0/kernel/src/ipc/endpoint.rs"
    snippet: "/// Receive a message from an endpoint.\n///\n/// If a sender is already waiting, dequeue it, wake it (if it used `send`\n/// rather than `call`), copy its message, and return the message label.\n/// If the endpoint is for a `call`, insert a reply capability into the\n/// server's table instead of waking the sender immediately.\n///\n/// If no sender is waiting, the calling task blocks until one arrives.\n///\n/// Returns the message label on success, or `u64::MAX` on error.\npub fn recv(receiver: TaskId, ep_id: EndpointId) -> u64 {\n    let action = {\n        let mut reg = ENDPOINTS.lock();\n        let ep = match reg.get_mut(ep_id) {\n            Some(e) => e,\n            None => return u64::MAX,\n        };\n        if let Some(pending) = ep.senders.pop_front() {\n            Some(pending)\n        } else {\n            // No sender yet — enqueue self and block.\n            ep.receivers.push_back(receiver);\n            None\n        }"
    snippetLanguage: "rust"
realOsDifferences:
  - "Real microkernels often include more message registers, stronger formal models, priority-aware scheduling interactions, and carefully tuned fast paths."
  - "Production systems like seL4 use formal verification to prove IPC correctness."
  - "This project keeps the IPC contract small and explicit so the reader can trace every state change during a message exchange."
successCriteria:
  - "A client can call a server and receive a reply."
  - "Invalid capabilities are rejected safely."
  - "Server loops use the intended `reply_recv` pattern."
  - "IRQ-driven notifications can wake a userspace task."
---

## Why this phase matters

Turn the kernel into a real microkernel by making userspace services communicate through explicit message passing and capabilities.

## How it works in m3OS

Phase 6 turns m³OS into a real microkernel by wiring up the IPC subsystem that all future userspace servers depend on.  By the end of this phase the kernel:
Everything from Phase 7 onward — the shell, console server, VFS — sits on top of this foundation.  If the IPC contract is clean, those higher layers are straightforward.  If it is muddled, nothing above it is trustworthy.

## How it builds on earlier phases

This phase builds on Userspace Entry and turns that foundation into IPC Core.

## Implementation outline

- Define kernel IPC objects and task wait states.
- Create a per-process capability table with explicit validation.
- Implement synchronous message transfer first.
- Add reply-and-wait server flow once the basic path is stable.
- Add notification objects for interrupts and one-way signals.

## Task progress snapshot

26 of 26 tracked tasks are complete.

## Documentation targets

- Documentation deliverables have not been listed yet.

## Deferred follow-on work

- Large page-grant transfers
- IPC timeouts and cancellation
- Advanced scheduling policies around IPC

## Source materials

- [Roadmap entry](https://github.com/mikecubed/m3os/blob/main/docs/roadmap/06-ipc-core.md)
- [Implementation document](https://github.com/mikecubed/m3os/blob/main/docs/06-ipc.md)
- [Task list](https://github.com/mikecubed/m3os/blob/main/docs/roadmap/tasks/06-ipc-core-tasks.md)
