---
phase: 5
title: "Userspace Entry"
status: complete
category: userspace
summary: "Run the first ring 3 program and prove that privilege separation, page permissions, and basic syscalls work."
learningGoal: "Understand the boundary between ring 0 and ring 3."
buildsOn:
  - "tasking"
extends: []
replaces: []
components:
  - "process-model"
  - "terminal"
learningObjectives:
  - "Understand the boundary between ring 0 and ring 3."
  - "Learn the minimum pieces needed for a userspace process."
  - "Introduce the syscall ABI used by the project."
keyFiles: []
codeSpotlights:
  - title: "Define a minimal process abstraction"
    file: "kernel/src/process/mod.rs"
    lines: "L9-L15"
    summary: "Define a minimal process abstraction. Why it matters: Processes need their own page tables, file descriptors, and lifecycle state — separate from the kernel task that executes them. Focus on `Process` and `ProcessState` in `kernel/src/process/mod.rs` while you trace the snippet."
    githubUrl: "https://github.com/mikecubed/m3os/blob/v0.5.0/kernel/src/process/mod.rs"
    snippet: "/// A minimal userspace process descriptor.\npub struct Process {\n    /// Virtual address of the process entry point.\n    pub entry: u64,\n    /// Virtual address of the process stack top.\n    pub stack_top: u64,\n}"
    snippetLanguage: "rust"
  - title: "Build a user address space"
    file: "kernel/src/mm/user_space.rs"
    lines: "L97-L111"
    summary: "Build a user address space. Why it matters: Userspace code must execute in its own virtual address space with code, stack, and kernel-protected mappings to enforce isolation. Focus on `copy_to_user` in `kernel/src/mm/user_space.rs` while you trace the snippet."
    githubUrl: "https://github.com/mikecubed/m3os/blob/v0.5.0/kernel/src/mm/user_space.rs"
    snippet: "/// Copy `src` bytes into the user code region at `virt_base`.\n///\n/// The region must already be mapped (e.g. via `map_user_pages`).\n/// The bounds check is sized to the **code** region (`USER_CODE_PAGES * 4096`);\n/// do not use this helper to write to the stack or any other user region.\npub fn copy_to_user(virt_base: u64, src: &[u8]) -> Result<(), &'static str> {\n    let max_bytes = (USER_CODE_PAGES * 4096) as usize;\n    if src.len() > max_bytes {\n        return Err(\"copy_to_user: src exceeds mapped user code region\");\n    }\n    // Safety: caller guarantees virt_base is mapped and we've checked the length.\n    let dst = unsafe { core::slice::from_raw_parts_mut(virt_base as *mut u8, src.len()) };\n    dst.copy_from_slice(src);\n    Ok(())\n}"
    snippetLanguage: "rust"
  - title: "Implement the transition into ring 3"
    file: "kernel/src/arch/x86_64/mod.rs"
    lines: "L28-L51"
    summary: "Implement the transition into ring 3. Why it matters: The ring transition is the security boundary — it drops privilege from ring 0 to ring 3 using iretq with the correct segment selectors. Focus on `enter_userspace` and `iretq` in `kernel/src/arch/x86_64/mod.rs` while you trace the snippet."
    githubUrl: "https://github.com/mikecubed/m3os/blob/v0.5.0/kernel/src/arch/x86_64/mod.rs"
    snippet: "/// Transfer execution to ring 3 (userspace).\n///\n/// Uses `iretq` to atomically switch to user code segment, user stack, and\n/// the given entry point with interrupts enabled (RFLAGS.IF = 1).\n///\n/// # Safety\n///\n/// * `entry` must be a valid, mapped, executable userspace virtual address.\n/// * `user_stack_top` must be a valid, mapped, writable userspace stack\n///   address (highest address; stack grows downward).\n/// * Must be called after `init()` so that GDT user segments are loaded.\npub unsafe fn enter_userspace(entry: u64, user_stack_top: u64) -> ! {\n    use core::arch::asm;\n    // Push the five values that `iretq` pops in order:\n    //   SS, RSP, RFLAGS, CS, RIP\n    // RFLAGS: bit 9 (IF) set → interrupts enabled in userspace.\n    asm!(\n        \"push {ss}\",\n        \"push {rsp}\",\n        \"push {rflags}\",\n        \"push {cs}\",\n        \"push {rip}\",\n        \"iretq\",\n        ss     = in(reg) u64::from(gdt::user_data_selector().0),"
    snippetLanguage: "rust"
realOsDifferences:
  - "Production kernels have richer executable loading, memory permissions, security policies, and per-thread user contexts. This milestone should focus on a tiny, single-purpose userspace program so the privilege transition remains easy to follow."
successCriteria:
  - "A userspace binary runs and returns control cleanly."
  - "Kernel pages are not writable from ring 3."
  - "The syscall path is documented and observable through logging."
  - "Faulting userspace code is reported without taking down the kernel silently."
---

## Why this phase matters

Run the first ring 3 program and prove that privilege separation, page permissions, and basic syscalls work.

## How it works in m3OS

Phase 5 delivers the first ring-3 program running on m³OS.  By the end of this phase the kernel:
This is the foundational step for everything in the userspace roadmap.  All later phases (IPC, servers, shell) build on the ring-3 execution model established here.

## How it builds on earlier phases

This phase builds on Tasking and turns that foundation into Userspace Entry.

## Implementation outline

- Create a process abstraction separate from kernel threads.
- Build a minimal userspace address space with code and stack mappings.
- Enter ring 3 through a carefully documented transition path.
- Install the syscall entry point and argument convention.
- Add one tiny userspace binary to exercise the path.

## Task progress snapshot

19 of 19 tracked tasks are complete.

## Documentation targets

- Documentation deliverables have not been listed yet.

## Deferred follow-on work

- dynamic ELF loading
- shared libraries
- full process lifecycle management

## Source materials

- [Roadmap entry](https://github.com/mikecubed/m3os/blob/main/docs/roadmap/05-userspace-entry.md)
- [Implementation document](https://github.com/mikecubed/m3os/blob/main/docs/05-userspace-entry.md)
- [Task list](https://github.com/mikecubed/m3os/blob/main/docs/roadmap/tasks/05-userspace-entry-tasks.md)
