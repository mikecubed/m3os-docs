---
phase: 31
title: "Compiler Bootstrap"
status: complete
category: productivity
summary: "Run a C compiler natively inside the OS. A C source file written at the shell prompt (using the editor from Phase 26) can be compiled and executed without leaving the OS. The ultimate target is self-hosting: the compiler compiles itself."
learningGoal: "Understand what \"bootstrapping\" means: deriving a tool from itself."
buildsOn:
  - "telnet-server"
extends: []
replaces: []
components:
  - "boot-path"
  - "process-model"
learningObjectives:
  - "Understand what \"bootstrapping\" means: deriving a tool from itself."
  - "See what a compiler actually needs from an OS: file I/O, heap, process execution."
  - "Learn why musl is the right libc target for a resource-constrained system."
  - "Experience the edit-compile-run cycle inside your own OS."
keyFiles: []
codeSpotlights:
  - title: "Obtain TCC source"
    file: "xtask/src/main.rs"
    lines: "L376-L384"
    summary: "**Symbol:** build_tcc"
    githubUrl: "https://github.com/mikecubed/m3os/blob/v0.31.1/xtask/src/main.rs"
    snippet: "///   target/tcc-staging/usr/src/hello.c      — test program\n///   target/tcc-staging/usr/src/tcc/         — TCC source for self-hosting\nfn build_tcc() -> Option<PathBuf> {\n    let root = workspace_root();\n    let staging = root.join(\"target/tcc-staging\");\n    let tcc_bin = staging.join(\"usr/bin/tcc\");\n\n    // Check if we already have a complete cached build. Validate sentinel\n    // artifacts to avoid reusing a partially-populated staging dir from an"
    snippetLanguage: "rust"
  - title: "sys_open FD lifecycle audit"
    file: "kernel/src/arch/x86_64/syscall.rs"
    lines: "L1233-L1241"
    summary: "**Symbol:** alloc_fd"
    githubUrl: "https://github.com/mikecubed/m3os/blob/v0.31.1/kernel/src/arch/x86_64/syscall.rs"
    snippet: "    };\n\n    let read_fd = match alloc_fd(3, read_entry) {\n        Some(fd) => fd,\n        None => {\n            // No FDs reference this pipe yet — free the slot directly.\n            crate::pipe::free_pipe(pipe_id);\n            return NEG_EMFILE;\n        }"
    snippetLanguage: "rust"
  - title: "Edit-compile-run cycle"
    file: "userspace/edit/src/main.rs"
    lines: "L1092-L1100"
    summary: "- userspace/edit/src/main.rs"
    githubUrl: "https://github.com/mikecubed/m3os/blob/v0.31.1/userspace/edit/src/main.rs"
    snippet: "// ---------------------------------------------------------------------------\n\nsyscall_lib::entry_point!(edit_main);\n\nfn edit_main(args: &[&str]) -> i32 {\n    register_sigwinch_handler();\n\n    let mut editor = Editor::new();"
    snippetLanguage: "rust"
realOsDifferences:
  - "Real systems ship with a full toolchain: compiler, assembler, linker, debugger, make, and a package manager. Bootstrapping a modern Linux distribution from source requires a carefully ordered multi-stage build (tarballs -> stage-1 gcc -> stage-2 gcc -> full system) documented by projects like Linux From Scratch. This phase achieves the same conceptual milestone — a system that can build and run its own tools — using TCC's single-file simplicity instead of a multi-stage GCC build."
successCriteria:
  - "`tcc --version` runs inside the OS and prints the version string."
  - "`hello.c` compiled by TCC inside the OS runs and prints `hello, world`."
  - "TCC successfully compiles itself inside the OS (self-hosting milestone)."
  - "The self-compiled `tcc2` passes the same `hello.c` test."
  - "No host tools are required after the disk image is built."
---

## Why this phase matters

Run a C compiler natively inside the OS. A C source file written at the shell prompt (using the editor from Phase 26) can be compiled and executed without leaving the OS. The ultimate target is self-hosting: the compiler compiles itself.

## How it works in m3OS

Phase 31 adds a native C compiler (TCC -- Tiny C Compiler) to m3OS. A C source file written with the text editor can be compiled and executed without leaving the OS. The ultimate milestone is self-hosting: TCC compiles itself inside the OS, producing a working compiler binary.

## How it builds on earlier phases

This phase builds on Telnet Server and turns that foundation into Compiler Bootstrap.

## Implementation outline

- On the host: build TCC from source with `./configure --prefix=/usr --cc=x86_64-linux-musl-gcc`.
- Verify the resulting binary is a static x86-64 ELF linked against musl.
- Add TCC binary, musl `libc.a`, and the musl headers to the disk image build in xtask.
- Boot the OS and verify `tcc --version` prints the expected string.
- Write a `hello.c` inside the OS using the editor, compile it, and run it.
- Compile a slightly larger program (e.g., a fibonacci calculator) to stress the heap.
- Attempt the self-hosting milestone: `tcc /usr/src/tcc/tcc.c -o /tmp/tcc2`.
- Verify the self-compiled TCC produces identical output for `hello.c`.

## Task progress snapshot

39 of 59 tracked tasks are complete.

## Documentation targets

- Explain what "bootstrapping" means and why it is a meaningful milestone.
- Document what TCC needs from the OS and how each syscall maps to OS functionality.
- Explain the musl libc build process and what headers/libs end up in the image.
- Document Path B and when it is the right choice vs. Path A.
- Write a short essay on the history of compiler bootstrapping (Ken Thompson's "Trusting Trust" lecture is the canonical reference).

## Deferred follow-on work

- GCC or Clang as the native compiler
- Dynamic linking and shared libraries
- Debugger support (`gdb` or `lldb`)
- Multi-stage bootstrap to remove host-compiled binaries from the chain entirely

## Source materials

- [Roadmap entry](https://github.com/mikecubed/m3os/blob/v0.31.1/docs/roadmap/31-compiler-bootstrap.md)
- [Implementation document](https://github.com/mikecubed/m3os/blob/v0.31.1/docs/31-compiler-bootstrap.md)
- [Task list](https://github.com/mikecubed/m3os/blob/v0.31.1/docs/roadmap/tasks/31-compiler-bootstrap-tasks.md)
