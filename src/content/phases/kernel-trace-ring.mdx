---
phase: 43
title: "Kernel Trace Ring"
status: complete
category: infrastructure
summary: "Every kernel crash includes a structured timeline of recent scheduler, fork, and IPC events. The per-core lockless trace ring records events with timestamps, and the auto-dump on panic/fault merges all cores into a single sorted timeline in the serial output. The ring is host-testable, accessible from userspace, and feature-gated for zero-cost release builds."
learningGoal: "Understand lockless per-core ring buffers and why they avoid contention"
buildsOn:
  - "crash-diagnostics"
extends: []
replaces: []
components:
  - "scheduler"
  - "process-model"
  - "ipc"
learningObjectives:
  - "Understand lockless per-core ring buffers and why they avoid contention"
  - "Understand how structured event enums enable machine-parseable crash dumps"
  - "Understand feature gating for zero-cost debug infrastructure"
  - "Understand syscall design for exposing kernel state to userspace"
keyFiles:
  - path: "kernel-core/src/trace_ring.rs"
    summary: "TraceRing<N>, TraceEvent, TraceEntry -- host-testable"
  - path: "kernel/src/trace.rs"
    summary: "trace_event(), dump_trace_rings()"
  - path: "kernel/src/smp/mod.rs"
    summary: "PerCoreData.trace_ring field"
  - path: "kernel/src/task/scheduler.rs"
    summary: "Scheduler instrumentation (6 sites)"
  - path: "kernel/src/process/mod.rs"
    summary: "Fork instrumentation (2 sites)"
  - path: "kernel/src/ipc/endpoint.rs"
    summary: "IPC instrumentation (7 sites)"
  - path: "kernel/src/arch/x86_64/syscall.rs"
    summary: "sys_ktrace syscall"
  - path: "userspace/syscall-lib/src/lib.rs"
    summary: "ktrace() userspace wrapper"
codeSpotlights:
  - title: "Define `TraceRing<const N: usize>` in kernel-core"
    file: "kernel-core/src/trace_ring.rs"
    lines: "L122-L132"
    summary: "Define `TraceRing<const N: usize>` in kernel-core. Why it matters: A lockless per-core ring avoids contention on the hot path; placing it in kernel-core makes it host-testable with standard cargo test just like the existing LogRing. Focus on `TraceRing` and `LogRing` in `kernel-core/src/trace_ring.rs` while you trace the snippet."
    githubUrl: "https://github.com/mikecubed/m3os/blob/v0.43.1/kernel-core/src/trace_ring.rs"
    snippet: "/// Per-core lockless circular trace buffer.\n///\n/// Holds the most recent `N` entries. New entries overwrite the oldest on wrap.\n/// No mutex — safe for single-writer (owning core) use.\npub struct TraceRing<const N: usize> {\n    buf: [TraceEntry; N],\n    write_idx: usize,\n    count: usize,\n}\n\nimpl<const N: usize> TraceRing<N> {"
    snippetLanguage: "rust"
  - title: "Per-core trace ring allocation in kernel"
    file: "kernel/src/smp/mod.rs"
    lines: "L56-L79"
    summary: "Per-core trace ring allocation in kernel. Why it matters: Each core needs its own ring to avoid lock contention; the ring must be sized to hold enough events to cover a typical fork+dispatch+fault sequence. Focus on `PerCoreData` and `TraceRing::new` in `kernel/src/smp/mod.rs` while you trace the snippet."
    githubUrl: "https://github.com/mikecubed/m3os/blob/v0.43.1/kernel/src/smp/mod.rs"
    snippet: "/// Per-core state block.\n///\n/// Each core has one of these, initialized during BSP init or AP bootstrap.\n/// The `gs_base` MSR points to this struct so that `per_core()` can retrieve\n/// it in O(1) without MMIO.\n#[repr(C)]\npub struct PerCoreData {\n    /// Self-pointer at offset 0 — reserved for future `gs:[0]` access.\n    /// Currently unused: `per_core()` reads `IA32_GS_BASE` via `rdmsr`.\n    self_ptr: *const PerCoreData,\n    /// Logical core index (0 = BSP, 1..n = APs in MADT order).\n    pub core_id: u8,\n    /// LAPIC ID from the MADT.\n    pub apic_id: u8,\n    /// Set to `true` once this core has completed initialization.\n    pub is_online: AtomicBool,\n    /// Pointer to this core's TSS (for runtime RSP0 updates).\n    tss_ptr: *mut TaskStateSegment,\n    /// Pointer to this core's GDT (pre-allocated on BSP, loaded on AP).\n    gdt_ptr: *const GlobalDescriptorTable,\n    /// Segment selectors for this core's GDT.\n    gdt_code: SegmentSelector,\n    gdt_data: SegmentSelector,\n    gdt_tss: SegmentSelector,"
    snippetLanguage: "rust"
  - title: "Trace emit helper"
    file: "kernel/src/trace.rs"
    lines: "L10-L32"
    summary: "Trace emit helper. Why it matters: A single trace_event(event) call site keeps instrumentation minimal (one line per call site) and handles timestamping and core-ID tagging automatically. Focus on `trace_event` and `trace_event(event)` in `kernel/src/trace.rs` while you trace the snippet."
    githubUrl: "https://github.com/mikecubed/m3os/blob/v0.43.1/kernel/src/trace.rs"
    snippet: "/// Emit a trace event on the current core's trace ring.\n///\n/// Compiles to nothing when the `trace` feature is off.\n#[cfg(feature = \"trace\")]\npub fn trace_event(event: TraceEvent) {\n    if !crate::smp::is_per_core_ready() {\n        return;\n    }\n    let core_id = crate::smp::per_core().core_id;\n    let tick = crate::arch::x86_64::interrupts::tick_count();\n    // Safety: trace_ring is wrapped in UnsafeCell for interior mutability.\n    // Only the owning core writes to its ring (single-writer guarantee via\n    // gs_base per-core data). The UnsafeCell makes the mutable access sound.\n    let ring_ptr = crate::smp::per_core().trace_ring.get();\n    unsafe {\n        (*ring_ptr).push(TraceEntry {\n            tick,\n            core: core_id,\n            _pad: [0; 7],\n            event,\n        });\n    }\n}"
    snippetLanguage: "rust"
realOsDifferences:
  - "Linux uses `ftrace` with per-CPU ring buffers backed by `ring_buffer.c`, supporting binary format, filtering, function graph tracing, and live streaming via `/sys/kernel/tracing/`"
  - "FreeBSD uses `DTrace` (and now also `ktrace`) with kernel-userspace cooperation and sophisticated filtering"
  - "Production systems use binary trace formats (CTF, perf.data) for efficient storage and offline analysis tools"
  - "Lock-free ring buffers in production kernels use memory ordering barriers and sequence counters for safe concurrent reader/writer access"
successCriteria:
  - "`cargo test -p kernel-core` passes with all trace ring tests"
  - "`cargo xtask check` passes with trace feature on and off"
  - "`cargo xtask test` passes with trace feature on"
  - "`cargo build -p kernel --no-default-features` compiles without trace code"
  - "Trace ring dump appears in serial output after crash diagnostics on panic"
  - "`sys_ktrace` syscall copies trace entries to userspace buffer"
---

## Why this phase matters

Every kernel crash includes a structured timeline of recent scheduler, fork, and IPC events. The per-core lockless trace ring records events with timestamps, and the auto-dump on panic/fault merges all cores into a single sorted timeline in the serial output. The ring is host-testable, accessible from userspace, and feature-gated for zero-cost release builds.

## How it works in m3OS

Phase 43b adds a per-core lockless trace ring that records scheduler, fork, and IPC events with timestamps. When the kernel crashes, the trace ring is automatically dumped alongside the crash diagnostics from Phase 43a, giving developers a structured timeline of the kernel transitions leading into the fault. The ring is host-testable in kernel-core, accessible from userspace via `sys_ktrace`, and feature-gated so it compiles out for release builds.

## How it builds on earlier phases

This phase builds on Crash Diagnostics and turns that foundation into Kernel Trace Ring.

## Implementation outline

- Implement `TraceRing<N>`, `TraceEvent`, `TraceEntry` in kernel-core
- Add host-side unit tests for the ring buffer
- Add `trace_ring` field to `PerCoreData` (BSP and AP init)
- Implement `trace_event()` and `dump_trace_rings()` in kernel
- Instrument scheduler paths (6 sites)
- Instrument fork paths (4 sites)
- Instrument IPC paths (7 sites)
- Wire `dump_trace_rings()` into panic/fault handlers
- Implement `sys_ktrace` syscall and syscall-lib wrapper
- Add feature gate (`trace` feature on kernel crate)
- Validate with `cargo xtask check` and `cargo xtask test`

## Task progress snapshot

0 of 76 tracked tasks are complete.

## Documentation targets

- Documentation deliverables have not been listed yet.

## Deferred follow-on work

- Binary trace format for off-target analysis tooling
- Userspace `ktrace` command-line tool
- Network-accessible trace export
- Trace filtering (per-subsystem enable/disable)
- Trace event for timer ISR / reschedule signal
- TSC-based sub-tick ordering

## Source materials

- [Roadmap entry](https://github.com/mikecubed/m3os/blob/main/docs/roadmap/43b-kernel-trace-ring.md)
- [Implementation document](https://github.com/mikecubed/m3os/blob/main/docs/43b-kernel-trace-ring.md)
- [Task list](https://github.com/mikecubed/m3os/blob/main/docs/roadmap/tasks/43b-kernel-trace-ring-tasks.md)
