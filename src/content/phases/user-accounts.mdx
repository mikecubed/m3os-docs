---
phase: 27
title: "User Accounts"
status: complete
category: userspace
summary: "The OS supports multiple user accounts with login authentication, file ownership, and permission enforcement. A `login` program prompts for username and password before granting shell access. This is the foundation for multi-user operation and secure remote access."
learningGoal: "Understand how Unix user/group identity works at the kernel level (UID/GID)."
buildsOn:
  - "text-editor"
extends: []
replaces: []
components:
  - "process-model"
  - "vfs"
  - "boot-path"
learningObjectives:
  - "Understand how Unix user/group identity works at the kernel level (UID/GID)."
  - "Learn how file permissions are checked on every `open`/`exec`/`mkdir` call."
  - "See how `setuid`/`setgid` enable privilege transitions."
  - "Understand password hashing — why plaintext passwords are never stored."
keyFiles:
  - path: "kernel/src/process/mod.rs"
    summary: "The Process struct (kernel/src/process/mod.rs) carries four"
  - path: "kernel/src/arch/x86_64/syscall.rs"
    summary: "kernel/src/arch/x86_64/syscall.rs:"
  - path: "kernel-core/src/fs/tmpfs.rs"
    summary: "FileData and DirData in kernel-core/src/fs/tmpfs.rs each hold"
  - path: "kernel/src/fs/ext2.rs"
    summary: "ext2 driver (kernel/src/fs/ext2.rs) reads and writes these fields"
  - path: "userspace/syscall-lib/src/sha256.rs"
    summary: "userspace/syscall-lib/src/sha256.rs (FIPS 180-4, no external"
  - path: "xtask/src/main.rs"
    summary: "populate_ext2_files() in xtask/src/main.rs, which uses debugfs"
  - path: "userspace/init/src/main.rs"
    summary: "Init (userspace/init/src/main.rs) was modified to:"
  - path: "userspace/login/"
    summary: "Rust no_std binary. Implements the full authentication cycle: prompt,"
  - path: "userspace/su/"
    summary: "Rust no_std binary. su <username> (defaults to root). Any user can"
  - path: "userspace/passwd/"
    summary: "setgid syscalls are unconditional until setuid-bit support is added.)"
  - path: "userspace/adduser/"
    summary: "Rust no_std binary. Root-only. Scans /etc/passwd for the maximum"
  - path: "userspace/id/"
    summary: "group files. Creates the home directory and sets ownership."
  - path: "userspace/whoami/"
    summary: "/etc/passwd and /etc/group, prints uid=N(name) gid=N(name)."
  - path: "userspace/syscall-lib/"
    summary: "Phase 27 added to userspace/syscall-lib/:"
codeSpotlights:
  - title: "Add UID/GID fields to Process struct"
    file: "kernel/src/process/mod.rs"
    lines: "L343-L366"
    summary: "Add UID/GID fields to Process struct. Why it matters: Every permission check and identity syscall reads these fields from the current process. Focus on `uid` and `gid` in `kernel/src/process/mod.rs` while you trace the snippet."
    githubUrl: "https://github.com/mikecubed/m3os/blob/v0.27.1/kernel/src/process/mod.rs"
    snippet: "/// Descriptor for a single userspace process.\npub struct Process {\n    /// This process's unique identifier.\n    pub pid: Pid,\n    /// Parent PID.  0 means no parent (init or an orphan).\n    pub ppid: Pid,\n    /// Current lifecycle state.\n    pub state: ProcessState,\n    /// Root physical address of this process's page table.\n    ///\n    /// `None` means the process has not yet been assigned a dedicated\n    /// address space and shares the kernel's mappings (pre-Phase 12).\n    pub page_table_root: Option<x86_64::PhysAddr>,\n    /// Top of this process's kernel-mode stack (virtual address).\n    pub kernel_stack_top: u64,\n    /// Userspace entry-point virtual address.\n    pub entry_point: u64,\n    /// Initial userspace RSP: the virtual address of `argc` on the ABI stack,\n    /// as returned by `setup_abi_stack`.  This is **not** the raw top of the\n    /// stack allocation — it points into the stack, below the argv/envp data.\n    pub user_stack_top: u64,\n    /// Exit code written when the process transitions to [`ProcessState::Zombie`].\n    pub exit_code: Option<i32>,\n    /// Signal that caused the process to stop (set when transitioning to Stopped)."
    snippetLanguage: "rust"
  - title: "Update identity syscalls to return real fields"
    file: "kernel/src/arch/x86_64/syscall.rs"
    lines: "L1446-L1452"
    summary: "Update identity syscalls to return real fields. Why it matters: Programs like id and whoami rely on these syscalls to report the current user. Focus on `sys_linux_getuid` and `sys_linux_getgid` in `kernel/src/arch/x86_64/syscall.rs` while you trace the snippet."
    githubUrl: "https://github.com/mikecubed/m3os/blob/v0.27.1/kernel/src/arch/x86_64/syscall.rs"
    snippet: "/// `getuid()` — return real user ID (syscall 102).\nfn sys_linux_getuid() -> u64 {\n    current_process_ids().0 as u64\n}\n\n/// `getgid()` — return real group ID (syscall 104).\nfn sys_linux_getgid() -> u64 {"
    snippetLanguage: "rust"
  - title: "Extend ramdisk nodes with metadata"
    file: "kernel/src/fs/ramdisk.rs"
    lines: "See file"
    summary: "Extend ramdisk nodes with metadata. Why it matters: Ramdisk files (initrd binaries) need permission bits so exec permission checks work. Focus on `uid` and `gid` in `kernel/src/fs/ramdisk.rs` while you trace the snippet."
    githubUrl: "https://github.com/mikecubed/m3os/blob/v0.27.1/kernel/src/fs/ramdisk.rs"
realOsDifferences:
  - "Real Unix systems use PAM (Pluggable Authentication Modules) for flexible auth backends."
  - "NSS (Name Service Switch) enables looking up users from LDAP, NIS, etc."
  - "Supplementary groups allow a user to belong to multiple groups."
  - "setuid/setgid binaries provide controlled privilege escalation."
  - "Fine-grained capabilities (Linux capabilities) replace the binary root vs. non-root model."
  - "SELinux/AppArmor provide mandatory access control beyond discretionary Unix permissions."
  - "Our implementation uses the classic Unix permission model (user/group/other with rwx bits), which is simple to understand and sufficient for learning."
successCriteria:
  - "Boot reaches a `login:` prompt instead of an immediate shell."
  - "Entering correct credentials for `root` drops into a root shell."
  - "Entering wrong credentials is rejected with an error message."
  - "`id` shows the correct UID/GID after login."
  - "Files created by `user` are owned by UID 1000."
  - "`user` cannot read `/etc/shadow` (permission denied)."
  - "`user` cannot delete files owned by `root` in protected directories."
  - "`su root` from `user` prompts for root's password and grants a root shell."
  - "`passwd` changes the current user's password, and the new password works on next login."
---

## Why this phase matters

The OS supports multiple user accounts with login authentication, file ownership, and permission enforcement. A `login` program prompts for username and password before granting shell access. This is the foundation for multi-user operation and secure remote access.

## How it works in m3OS

Phase 27 adds multi-user support to m3OS: per-process UID/GID identity, file ownership and Unix permission enforcement across all filesystem backends, SHA-256 password hashing, and a login-based boot flow. Six new userspace programs (`login`, `su`, `passwd`, `adduser`, `id`, `whoami`) provide authentication and account management.
After Phase 27, the system boots to a `login:` prompt instead of dropping directly into a shell. Two default accounts exist: `root` (UID 0) and `user` (UID 1000).

## How it builds on earlier phases

This phase builds on Text Editor and turns that foundation into User Accounts.

## Implementation outline

- Add UID/GID fields to the kernel process structure; default to 0 (root) for init.
- Implement `getuid`, `setuid`, and related syscalls.
- Add permission mode and owner fields to VFS inodes.
- Implement permission checking in VFS operations.
- Implement `chmod` and `chown` syscalls.
- Write the `login` program: read `/etc/passwd`, verify password hash from `/etc/shadow`.
- Modify init to spawn `login` instead of directly spawning the shell.
- Write `su`, `passwd`, `id`, `whoami`, `adduser` utilities.
- Create default `/etc/passwd`, `/etc/shadow`, `/etc/group` in the disk image.

## Task progress snapshot

86 of 86 tracked tasks are complete.

## Documentation targets

- Documentation deliverables have not been listed yet.

## Deferred follow-on work

- ext2 filesystem (Phase 28)
- PAM or pluggable authentication
- Supplementary groups
- ACLs (Access Control Lists)
- Mandatory access control (SELinux-style)
- Home directory creation on first login
- User quotas
- Account lockout after failed attempts

## Source materials

- [Roadmap entry](https://github.com/mikecubed/m3os/blob/main/docs/roadmap/27-user-accounts.md)
- [Implementation document](https://github.com/mikecubed/m3os/blob/main/docs/27-user-accounts.md)
- [Task list](https://github.com/mikecubed/m3os/blob/main/docs/roadmap/tasks/27-user-accounts-tasks.md)
