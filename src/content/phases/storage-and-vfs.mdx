---
phase: 8
title: "Storage And VFS"
status: complete
category: infrastructure
summary: "Expose files through userspace services so programs can read named resources instead of relying on embedded data or ad hoc kernel hooks."
learningGoal: "Understand how a microkernel can provide filesystem access through servers."
buildsOn:
  - "core-servers"
extends: []
replaces: []
components:
  - "vfs"
learningObjectives:
  - "Understand how a microkernel can provide filesystem access through servers."
  - "Learn path routing and mount-like behavior without building a full POSIX layer."
  - "Keep storage read-only at first to reduce complexity."
keyFiles:
  - path: "kernel/src/fs/protocol.rs"
    summary: "Phase 8 defines three label numbers in kernel/src/fs/protocol.rs. All use the existing"
  - path: "kernel/initrd/hello.txt"
    summary: "The paths kernel/initrd/hello.txt and kernel/initrd/readme.txt must exist in the repository"
  - path: "kernel/initrd/readme.txt"
    summary: "The paths kernel/initrd/hello.txt and kernel/initrd/readme.txt must exist in the repository"
codeSpotlights:
  - title: "Define a file-oriented IPC contract for open and read"
    file: "kernel/src/fs/protocol.rs"
    lines: "See file"
    summary: "Define a file-oriented IPC contract for open and read. Why it matters: A stable IPC contract decouples file consumers from filesystem implementation details. Focus on `kernel/src/fs/protocol.rs` while you trace the snippet."
    githubUrl: "https://github.com/mikecubed/m3os/blob/v0.8.0/kernel/src/fs/protocol.rs"
  - title: "Implement vfs_server as a path router"
    file: "kernel/src/fs/vfs.rs"
    lines: "See file"
    summary: "Implement vfs_server as a path router. Why it matters: The VFS layer routes path requests to the correct backend, keeping filesystem-specific logic isolated. Focus on `kernel/src/fs/vfs.rs` while you trace the snippet."
    githubUrl: "https://github.com/mikecubed/m3os/blob/v0.8.0/kernel/src/fs/vfs.rs"
  - title: "Implement a read-only filesystem backend"
    file: "kernel/src/fs/fat32.rs"
    lines: "L176-L189"
    summary: "Implement a read-only filesystem backend. Why it matters: A real filesystem backend lets the OS read files from the boot media rather than only embedded data. Focus on `Fat32Volume` and `mount_fat32` in `kernel/src/fs/fat32.rs` while you trace the snippet."
    githubUrl: "https://github.com/mikecubed/m3os/blob/v0.8.0/kernel/src/fs/fat32.rs"
    snippet: "/// A mounted FAT32 volume backed by virtio-blk sectors.\npub struct Fat32Volume {\n    pub bpb: Fat32Bpb,\n    /// Absolute LBA of the partition start on the block device.\n    base_lba: u64,\n    /// Absolute LBA of the first FAT sector.\n    fat_start_lba: u64,\n    /// Absolute LBA of the first data cluster (cluster 2).\n    data_start_lba: u64,\n    /// Hint for next free cluster search.\n    alloc_hint: u32,\n}\n\n/// Global mounted FAT32 volume (set by mount_fat32)."
    snippetLanguage: "rust"
realOsDifferences:
  - "Mature operating systems add caching, writeback, permissions, journaling, block-layer abstractions, and many filesystem drivers."
  - "A toy OS starts with read-only access because it teaches layering and naming without dragging in crash consistency problems."
  - "Production VFS layers (Linux, BSD) support dozens of filesystem types, mount namespaces, and complex permission models."
successCriteria:
  - "A userspace program can open a file by path and read its contents."
  - "The VFS and filesystem backend have clear ownership boundaries."
  - "Failures such as missing files are reported predictably."
  - "The disk-image content used for demos is documented."
---

## Why this phase matters

Expose files through userspace services so programs can read named resources instead of relying on embedded data or ad hoc kernel hooks.

## How it works in m3OS

Phase 8 adds the first file-access layer to m³OS. The additions are:
### Why route file access through IPC?
The same policy/mechanism argument from Phase 7 applies here. The kernel should own only the mechanism — endpoint delivery, scheduling, memory protection. Policy questions (which process can read which path, how to handle missing files, what backends exist) belong in userspace servers.
Routing through `vfs_server` instead of letting clients call `fat_server` directly also creates the ownership boundary needed for Phase 9+: adding a second backend (tmpfs, network share) only requires changing `vfs_server`'s routing table, not every client.

## How it builds on earlier phases

This phase builds on Core Servers and turns that foundation into Storage And VFS.

## Implementation outline

- Define a small file-oriented IPC protocol.
- Build `vfs_server` as a router instead of a full filesystem implementation.
- Add one filesystem backend with a narrow scope.
- Place a few test files in the boot media and verify they can be read.
- Keep caching and mutation out of the first milestone.

## Task progress snapshot

14 of 14 tracked tasks are complete.

## Documentation targets

- Documentation deliverables have not been listed yet.

## Deferred follow-on work

- Writable filesystems
- Page cache and buffering
- Permissions and access control

## Source materials

- [Roadmap entry](https://github.com/mikecubed/m3os/blob/main/docs/roadmap/08-storage-and-vfs.md)
- [Implementation document](https://github.com/mikecubed/m3os/blob/main/docs/08-storage-and-vfs.md)
- [Task list](https://github.com/mikecubed/m3os/blob/main/docs/roadmap/tasks/08-storage-and-vfs-tasks.md)
