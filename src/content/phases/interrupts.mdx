---
phase: 3
title: "Interrupts"
status: complete
category: foundations
summary: "Handle exceptions and hardware interrupts reliably enough to support timing, keyboard input, and basic fault diagnosis."
learningGoal: "Understand the IDT, PIC, and interrupt stacks."
buildsOn:
  - "memory-basics"
extends: []
replaces: []
components:
  - "interrupt-router"
learningObjectives:
  - "Understand the IDT, PIC, and interrupt stacks."
  - "Separate exception handling from device IRQ handling."
  - "Keep interrupt handlers minimal and deterministic."
keyFiles: []
codeSpotlights:
  - title: "Set up the GDT, TSS, and interrupt stack entries"
    file: "kernel/src/arch/x86_64/gdt.rs"
    lines: "L55-L61"
    summary: "Set up the GDT, TSS, and interrupt stack entries. Why it matters: The GDT defines segment descriptors for kernel and user code/data, and the TSS provides the interrupt stack table (IST) entries needed for safe double-fault handling. Focus on `GDT` and `TSS` in `kernel/src/arch/x86_64/gdt.rs` while you trace the snippet."
    githubUrl: "https://github.com/mikecubed/m3os/blob/v0.3.0/kernel/src/arch/x86_64/gdt.rs"
    snippet: "static GDT: Lazy<(GlobalDescriptorTable, Selectors)> = Lazy::new(|| {\n    let mut gdt = GlobalDescriptorTable::new();\n    let code = gdt.append(Descriptor::kernel_code_segment());\n    let data = gdt.append(Descriptor::kernel_data_segment());\n    let tss = gdt.append(Descriptor::tss_segment(&TSS));\n    (gdt, Selectors { code, data, tss })\n});"
    snippetLanguage: "rust"
  - title: "Build the IDT and install exception handlers"
    file: "kernel/src/arch/x86_64/interrupts.rs"
    lines: "L14-L36"
    summary: "Build the IDT and install exception handlers. Why it matters: Without an IDT, any CPU exception causes a triple fault and immediate reset rather than a debuggable error message. Focus on `IDT` and `init` in `kernel/src/arch/x86_64/interrupts.rs` while you trace the snippet."
    githubUrl: "https://github.com/mikecubed/m3os/blob/v0.3.0/kernel/src/arch/x86_64/interrupts.rs"
    snippet: "static IDT: Lazy<InterruptDescriptorTable> = Lazy::new(|| {\n    let mut idt = InterruptDescriptorTable::new();\n\n    // CPU exceptions\n    idt.breakpoint.set_handler_fn(breakpoint_handler);\n    idt.page_fault.set_handler_fn(page_fault_handler);\n    idt.general_protection_fault\n        .set_handler_fn(general_protection_fault_handler);\n    unsafe {\n        idt.double_fault\n            .set_handler_fn(double_fault_handler)\n            .set_stack_index(gdt::DOUBLE_FAULT_IST_INDEX);\n    }\n\n    // Hardware IRQs\n    idt[InterruptIndex::Timer as u8].set_handler_fn(timer_handler);\n    idt[InterruptIndex::Keyboard as u8].set_handler_fn(keyboard_handler);\n\n    idt\n});\n\n/// Load the IDT.\npub fn init() {"
    snippetLanguage: "rust"
realOsDifferences:
  - "Modern kernels often use APICs, MSI/MSI-X, more sophisticated interrupt routing, and complex per-CPU data structures. This project should stay with the simpler PIC path until the reader understands exceptions, IRQ acknowledgment, and stack discipline."
successCriteria:
  - "Breakpoints and page faults produce readable diagnostics."
  - "Timer interrupts occur at the expected cadence."
  - "Keyboard input reaches a buffer or log output."
  - "Interrupt handlers do not allocate or block."
---

## Why this phase matters

Handle exceptions and hardware interrupts reliably enough to support timing, keyboard input, and basic fault diagnosis.

## How it works in m3OS

Interrupts allow the CPU to respond to asynchronous events (hardware I/O, timers) and synchronous faults (invalid memory access, division by zero). The kernel configures the **Interrupt Descriptor Table (IDT)** — a table of 256 handlers that the CPU indexes into when an event fires.

## How it builds on earlier phases

This phase builds on Memory Basics and turns that foundation into Interrupts.

## Implementation outline

- Build the GDT, TSS, and IDT setup sequence.
- Add exception handlers for breakpoint, page fault, and double fault.
- Initialize the PIC with a clean vector layout.
- Add timer and keyboard IRQ handlers.
- Push non-trivial work out of the IRQ path into buffers or later phases.

## Task progress snapshot

19 of 19 tracked tasks are complete.

## Documentation targets

- Documentation deliverables have not been listed yet.

## Deferred follow-on work

- APIC and SMP interrupt routing
- advanced driver interrupt models
- complex deferred work queues

## Source materials

- [Roadmap entry](https://github.com/mikecubed/m3os/blob/main/docs/roadmap/03-interrupts.md)
- [Implementation document](https://github.com/mikecubed/m3os/blob/main/docs/03-interrupts.md)
- [Task list](https://github.com/mikecubed/m3os/blob/main/docs/roadmap/tasks/03-interrupts-tasks.md)
