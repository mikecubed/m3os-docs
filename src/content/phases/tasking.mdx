---
phase: 4
title: "Tasking"
status: complete
category: foundations
summary: "Introduce multiple concurrent execution contexts in the kernel and make timer-driven preemption visible and understandable."
learningGoal: "Understand what a task context actually contains."
buildsOn:
  - "interrupts"
extends: []
replaces: []
components:
  - "scheduler"
learningObjectives:
  - "Understand what a task context actually contains."
  - "Learn how timer interrupts interact with scheduling."
  - "Build intuition for cooperative versus preemptive execution."
keyFiles: []
codeSpotlights:
  - title: "Define the kernel task structure and task states"
    file: "kernel/src/task/mod.rs"
    lines: "L70-L78"
    summary: "**Symbols:** Task, TaskState"
    githubUrl: "https://github.com/mikecubed/m3os/blob/main/kernel/src/task/mod.rs"
    snippet: "\n// ---------------------------------------------------------------------------\n// Task ID\n// ---------------------------------------------------------------------------\n\n// TaskId is re-exported from kernel_core::types above.\n\n// ---------------------------------------------------------------------------\n// Task state"
    snippetLanguage: "rust"
  - title: "Add a round-robin scheduler and ready queue"
    file: "kernel/src/task/scheduler.rs"
    lines: "L29-L37"
    summary: "**Symbol:** Scheduler"
    githubUrl: "https://github.com/mikecubed/m3os/blob/main/kernel/src/task/scheduler.rs"
    snippet: "// ---------------------------------------------------------------------------\n\npub(super) static SCHEDULER: Mutex<Scheduler> = Mutex::new(Scheduler::new());\n\n// ---------------------------------------------------------------------------\n// Per-core helpers\n// ---------------------------------------------------------------------------\n\n/// Get the current core's reschedule flag."
    snippetLanguage: "rust"
  - title: "Trigger scheduling from the timer interrupt"
    file: "kernel/src/arch/x86_64/interrupts.rs"
    lines: "L303-L311"
    summary: "**Symbol:** timer_handler"
    githubUrl: "https://github.com/mikecubed/m3os/blob/main/kernel/src/arch/x86_64/interrupts.rs"
    snippet: "\n    // Hardware IRQs\n    idt[InterruptIndex::Timer as u8].set_handler_fn(timer_handler);\n    idt[InterruptIndex::Keyboard as u8].set_handler_fn(keyboard_handler);\n    idt[InterruptIndex::VirtioNet as u8].set_handler_fn(virtio_net_handler);\n\n    // APIC spurious interrupt vector — must NOT send EOI.\n    idt[InterruptIndex::Spurious as u8].set_handler_fn(spurious_handler);"
    snippetLanguage: "rust"
realOsDifferences:
  - "Mature schedulers handle priorities, CPU affinity, latency targets, and many blocking sources. A toy round-robin scheduler is intentionally unsophisticated because it makes context switching and task states easy to inspect and reason about."
successCriteria:
  - "At least two kernel tasks can run and interleave output."
  - "Register state survives task switches."
  - "The idle task runs when there is no ready work."
  - "Scheduler behavior is simple enough to explain from logs."
---

## Why this phase matters

Introduce multiple concurrent execution contexts in the kernel and make timer-driven preemption visible and understandable.

## How it works in m3OS

Phase 4 introduces cooperative multi-tasking to the kernel.  Each **task** is an independent unit of execution with its own stack.  The kernel switches between tasks by saving and restoring a small register frame — the **context** — without involving the CPU's hardware task-switching mechanism (which is too heavy-weight for a microkernel).

## How it builds on earlier phases

This phase builds on Interrupts and turns that foundation into Tasking.

## Implementation outline

- Define the task model and saved register layout.
- Create kernel stacks for spawned tasks.
- Implement the assembly context switch with a narrow ABI.
- Add a ready queue and a simple scheduler.
- Trigger rescheduling from timer interrupts.

## Task progress snapshot

18 of 18 tracked tasks are complete.

## Documentation targets

- Documentation deliverables have not been listed yet.

## Deferred follow-on work

- priorities and deadline scheduling
- sleep queues and timers beyond the basic tick
- SMP-aware run queues

## Source materials

- [Roadmap entry](https://github.com/mikecubed/m3os/blob/main/docs/roadmap/04-tasking.md)
- [Implementation document](https://github.com/mikecubed/m3os/blob/main/docs/05-tasking.md)
- [Task list](https://github.com/mikecubed/m3os/blob/main/docs/roadmap/tasks/04-tasking-tasks.md)
