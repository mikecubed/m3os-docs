---
phase: 4
title: "Tasking"
status: complete
category: foundations
summary: "Introduce multiple concurrent execution contexts in the kernel and make timer-driven preemption visible and understandable."
learningGoal: "Understand what a task context actually contains."
buildsOn:
  - "interrupts"
extends: []
replaces: []
components:
  - "scheduler"
learningObjectives:
  - "Understand what a task context actually contains."
  - "Learn how timer interrupts interact with scheduling."
  - "Build intuition for cooperative versus preemptive execution."
keyFiles: []
codeSpotlights:
  - title: "Define the kernel task structure and task states"
    file: "kernel/src/task/mod.rs"
    lines: "L80-L92"
    summary: "Define the kernel task structure and task states. Why it matters: Every schedulable unit needs a descriptor holding its saved registers, state, stack pointer, and metadata. Focus on `Task` and `TaskState` in `kernel/src/task/mod.rs` while you trace the snippet."
    githubUrl: "https://github.com/mikecubed/m3os/blob/v0.4.0/kernel/src/task/mod.rs"
    snippet: "pub struct Task {\n    /// Unique task identifier — not yet read outside this module.\n    #[allow(dead_code)]\n    pub id: TaskId,\n    /// Human-readable name — not yet read outside this module.\n    #[allow(dead_code)]\n    pub name: &'static str,\n    pub state: TaskState,\n    /// RSP saved by `switch_context` when this task is not running.\n    pub saved_rsp: u64,\n    /// Owns the allocated kernel stack — dropped when the `Task` is dropped.\n    _stack: Box<[u8]>,\n}"
    snippetLanguage: "rust"
  - title: "Add a round-robin scheduler and ready queue"
    file: "kernel/src/task/scheduler.rs"
    lines: "L53-L64"
    summary: "Add a round-robin scheduler and ready queue. Why it matters: The scheduler determines which task runs next and drives all cooperative and preemptive multitasking. Focus on `Scheduler` and `Ready` in `kernel/src/task/scheduler.rs` while you trace the snippet."
    githubUrl: "https://github.com/mikecubed/m3os/blob/v0.4.0/kernel/src/task/scheduler.rs"
    snippet: "struct Scheduler {\n    tasks: Vec<Task>,\n    /// Index of the last non-idle task that was dispatched (for round-robin).\n    last_run: usize,\n    /// Index of the task currently holding the CPU, or `None` while the\n    /// scheduler loop itself is running.\n    current: Option<usize>,\n    /// Index of the dedicated idle task in `tasks`, registered via\n    /// [`spawn_idle`].  The idle task is excluded from the normal round-robin\n    /// rotation and is selected only when no non-idle task is `Ready`.\n    idle_task: Option<usize>,\n}"
    snippetLanguage: "rust"
  - title: "Trigger scheduling from the timer interrupt"
    file: "kernel/src/arch/x86_64/interrupts.rs"
    lines: "L121-L128"
    summary: "Trigger scheduling from the timer interrupt. Why it matters: Timer-driven preemption ensures no single task monopolizes the CPU, which is the foundation of a multitasking OS. Focus on `timer_handler` and `signal_reschedule` in `kernel/src/arch/x86_64/interrupts.rs` while you trace the snippet."
    githubUrl: "https://github.com/mikecubed/m3os/blob/v0.4.0/kernel/src/arch/x86_64/interrupts.rs"
    snippet: "extern \"x86-interrupt\" fn timer_handler(_stack_frame: InterruptStackFrame) {\n    TICK_COUNT.fetch_add(1, Ordering::Relaxed);\n    crate::task::signal_reschedule();\n    unsafe {\n        PICS.lock()\n            .notify_end_of_interrupt(InterruptIndex::Timer as u8);\n    }\n}"
    snippetLanguage: "rust"
realOsDifferences:
  - "Mature schedulers handle priorities, CPU affinity, latency targets, and many blocking sources. A toy round-robin scheduler is intentionally unsophisticated because it makes context switching and task states easy to inspect and reason about."
successCriteria:
  - "At least two kernel tasks can run and interleave output."
  - "Register state survives task switches."
  - "The idle task runs when there is no ready work."
  - "Scheduler behavior is simple enough to explain from logs."
---

## Why this phase matters

Introduce multiple concurrent execution contexts in the kernel and make timer-driven preemption visible and understandable.

## How it works in m3OS

Phase 4 introduces cooperative multi-tasking to the kernel.  Each **task** is an independent unit of execution with its own stack.  The kernel switches between tasks by saving and restoring a small register frame — the **context** — without involving the CPU's hardware task-switching mechanism (which is too heavy-weight for a microkernel).

## How it builds on earlier phases

This phase builds on Interrupts and turns that foundation into Tasking.

## Implementation outline

- Define the task model and saved register layout.
- Create kernel stacks for spawned tasks.
- Implement the assembly context switch with a narrow ABI.
- Add a ready queue and a simple scheduler.
- Trigger rescheduling from timer interrupts.

## Task progress snapshot

18 of 18 tracked tasks are complete.

## Documentation targets

- Documentation deliverables have not been listed yet.

## Deferred follow-on work

- priorities and deadline scheduling
- sleep queues and timers beyond the basic tick
- SMP-aware run queues

## Source materials

- [Roadmap entry](https://github.com/mikecubed/m3os/blob/main/docs/roadmap/04-tasking.md)
- [Implementation document](https://github.com/mikecubed/m3os/blob/main/docs/04-tasking.md)
- [Task list](https://github.com/mikecubed/m3os/blob/main/docs/roadmap/tasks/04-tasking-tasks.md)
